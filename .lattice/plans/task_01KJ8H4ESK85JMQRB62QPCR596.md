# HAR-7: Smooth ship position interpolation

Wire up the existing lerp utilities to smoothly interpolate ship positions between AIS updates. Ships should glide rather than jump when new positions arrive.
