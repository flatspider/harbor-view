# HAR-38: Render NOAA shoreline lines only (no land polygons)

Disable runtime land polygon rendering in the harbor scene and retain only NOAA shoreline line overlay, then clear active land polygon data file to an empty FeatureCollection and verify TypeScript build.
