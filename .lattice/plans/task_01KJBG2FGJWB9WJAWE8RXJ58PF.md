# HAR-68: Add 4 new environmental data sources: NOAA PORTS, Moon Phase, Pressure Fog, Ferry Routes

Plan from user-approved ROADMAP implementation. Four features:
1. NOAA PORTS sensor data (air temp, water temp, pressure, wind) — server + environment + UI
2. Moon phase computation — pure math module + server snapshot + sky/atmosphere effects + UI
3. Pressure-driven fog + spring tide wave boost — atmosphere + ocean enhancements
4. NYC Ferry route lines — GTFS build script + scene renderer
