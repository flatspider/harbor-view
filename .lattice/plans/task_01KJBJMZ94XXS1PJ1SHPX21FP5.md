# Plan for HAR-70

Compute a zoom-based ship visual scale from camera distance in `HarborScene.tsx`, pass it into `animateShips`, and apply it per marker in `ships.ts` so vessels are larger when zoomed out and return to realistic size when zoomed in.
