# HAR-76: Add ship-category sprites and texture pipeline

Implement category-specific ship imagery using the existing `ShipCategory` mapping (special, passenger, cargo, tanker, other), add a reusable category→texture mapping and loader, and attach per-category sprites to vessels in the Three.js harbor scene as a first step toward a Ghibli-style art pipeline.
