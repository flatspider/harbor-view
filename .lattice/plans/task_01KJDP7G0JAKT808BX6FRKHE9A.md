# HAR-81: Restore all-ferry visual mode after performance optimization

Restore `FORCE_FERRY_MODEL_FOR_ALL_SHIPS` in `src/scene/ships.ts` so all vessels render with the ferry model again, then run `bun run build` to verify no regressions.
